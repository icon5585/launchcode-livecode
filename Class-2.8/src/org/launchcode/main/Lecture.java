package org.launchcode.main;

public class Lecture {

	/**
	 * Class 2.6 - 2/3/2020 - Chapter 9/10 - Intro to Spring & Controllers - Hank DeDona
	 * 
	 * @param args arguments passed into app at runtime
	 */
	public static void main(String[] args) {
		// 9.1.3 - Welcome to MVC
		
		// Model, View & Controller
		// Model - Handles the data, represents it, usually POJOs
		// View - Handles the UI, presents the data to the user
		// Controller - The part of the web app that handles requests, passes information to/from views/models
		
		// Query params (GETs)
		// Query params are anything followed by the ? in a web url and are in key=value pairs 
		
		// Path params (GETs)
		// Path params are part of the URL itself and can be /hello/world/billy 
		
		// Forms
		// Allows users to type in/provide input to server (POSTs)
		
		// Setting up spring app
		// Creating a new spring boot app is simple with the initializer
		// https://start.spring.io/
		
		// 10.1 - Simple Controllers
		// See class HelloController.java in hello-spring project
	}

}
